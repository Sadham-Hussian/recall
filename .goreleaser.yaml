version: 2

project_name: recall

builds:
  - binary: recall
    goos:
      - darwin
    goarch:
      - amd64
      - arm64
    tags:
      - sqlite_fts5
    env:
      - CGO_ENABLED=1

  - binary: recall
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    tags:
      - sqlite_fts5
    env:
      - CGO_ENABLED=1

archives:
  - format: tar.gz

checksum:
  name_template: "checksums.txt"

release:
  github:
    owner: Sadham-Hussian
    name: recall